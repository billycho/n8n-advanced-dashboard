Create ai agents features
use /features/products as the guide

on features/ai-agents there will be:
components/add-ai-agent-dialog.tsx
api.ts
hooks.ts
server.ts
types.ts

also add the models too: AIAgents.ts, this is the table 
AI Agents:
name
description
category
environment (dev, staging, prod)
webhook_url
model (gpt-4, gpt-5)
last_run_at TIMESTAMP
slug (short link)

also create the api on app/api: 
ai-agents/route.ts

also on all generated files add update and delete features too (currently on products there is only create and get)

dont change anything, only create the ai agents features

//
update the ai agent page now
use app/dashboard/basic-crud as the guide
update the ai agent page on app/dashboard/ai-agents.tsx 

also add create ai agent button that will display the popup from features/ai-agents/components/add-ai-agent-dialog.tsx
also add update and delete on the table too, it will be lkie this, there will be 3 dot icon on each row if I push the 3 dot icon, there will be selection of update or delete, make it as clean as possible (natural as possible) 

//
add webhook_url (optional) to the add-ai-agent-dialog.tsx
create ai-agents/components/view-ai-agent-dialog.tsx

add detail button on each row of the table in dashboard/ai-agents/page.tsx
when I press the detail button it will show view-ai-agennt-dialog.tsx

add update button on each row of the table in dashboard/ai-agents/page.tsx
when I press the update button it will show update-ai-agent-dialog.tsx


//
create a new page on dashboard/ai-agents 
the url will be 
dashboard/ai-agents/{id}
the id is the ai agent id

on this new page:
display the ai agents information all of them also with functional update and delete button (all of them will be popup that use @root/features/ai-agents/components/)


on ai agent table on ai-agents/page.tsx
 <TableRow
        key={agent._id}
        onClick={() => handleView(agent)}
onclick will redirect to dashboard/ai-agents/{id}

//

on this page:
dashboard/ai-agents/{id}

below the ai agent detail
create n8n workflow details UI (functional UI only)
the info is
Workflow Status (Enabled or disabled), if disable create a button to enable it that will popup confirmation, same with if its enabled, create button to disable it and popup confirmation too
Workflow Schedule time => create popup to edit the schedule time

Below the n8n workflow details UI
create the n8n workflow parameters where user can do CRUD on the parameter (for testing purpose just use local storage)


//
on app\dashboard\ai-agents\[id]\page.tsx
connect the n8n workflow settings and the parameter to this API
https://n8n.nodemationhub.com/api/v1/workflows/pBFu271nBy82vsKS?excludePinnedData=true
replace pBFu271nBy82vsKS with the n8n workflow id on the page, we will use this workflow id to fetch and update workflow data from the API
the header for this api is => X-N8N-API-KEY
the value of the X-N8N-API-KEY is eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI4Nzc2ZDYyNS05NjhjLTRkYTctODRmMy1hZWQ0YTE0NzI2YmIiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwiaWF0IjoxNzc3NTE0MTI4fQ.LKHG98_MZNFT6s4ct7ESLmugeBaRw3Bk5Gr6Fl52HxM

the response structure is:
documents\workflow_api_response.json

also create the workflow folder based on features\ai-agents
so the folder will be features\workflows
on workflow folder there will be 
components
api.ts this one use external API https://n8n.nodemationhub.com/api/v1/workflows/pBFu271nBy82vsKS?excludePinnedData=true
hooks.ts
types.ts

no need for server.ts since we connect to external APO

on app\dashboard\ai-agents\[id]\page.tsx, we will use files from features\workflows similar with how the page.tsx use features\ai-agents

focus on fetching the data first (the correct parameter and stuff)
for the workflow status we get from (file is documents\workflow_api_response.json)
    "active": true

for parameter we get from 
{
            "parameters": {
                "assignments": {
                    "assignments": [
                        {
                            "id": "08ac7fdb-bdcd-4212-a054-784bb11c3bc6",
                            "name": "param1",
                            "value": "test 1",
                            "type": "string"
                        },
                        {
                            "id": "b036f549-52b7-402c-b3c8-61f0eab8df3d",
                            "name": "param2",
                            "value": "test 2",
                            "type": "string"
                        }
                    ]
                },
                "options": {}
            },
            "type": "n8n-nodes-base.set",
            "typeVersion": 3.4,
            "position": [
                160,
                -48
            ],
            "id": "817f0280-b94c-4758-917b-48d51f9049e6",
            "name": "Set Data"
        }

schedule is from
{
            "parameters": {
                "rule": {
                    "interval": [
                        {
                            "field": "cronExpression",
                            "expression": "1 1 * * *"
                        }
                    ]
                }
            },
            "type": "n8n-nodes-base.scheduleTrigger",
            "typeVersion": 1.3,
            "position": [
                -160,
                112
            ],
            "id": "587c37a8-1365-4c68-b644-21c0613f1ea0",
            "name": "Schedule Trigger"
        },

Basically we only need to edit the cron expression (on the page we also directly edit the cronexpression)

